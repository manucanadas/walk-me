const initialState = {
  loggedIn: true,
  user: {
    id: 1,
    username: 'walkymcwalkface'
  }
}

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default reducer
